{{!
  @TODO:
    1. create common template for all pages (About, Contact, Welcome, etc...) - they will contain navbar, content and footer
    2. use different template for network (no footer, different navbar behavior
}}

<div class="c-notifications__container">
    {{#each notifications as |notification|}}
        {{notification-message notification=notification}}
    {{/each}}
</div>

{{elvis-nav dropMenu=dropMenu loginVisible=loginVisible}}

{{outlet}}
{{partial footer}}

{{!-- ember-cli-materialize modal container --}}
{{md-modal-container classNames=modalWrapperClass}}
