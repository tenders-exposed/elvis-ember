<div class="jumbotron">
    <div class="container text-center">
        <h1>Howdy, data journalists</h1>
        <p class="lead"><strong>el.vis</strong> is an investigative visualisation tool to help you find patterns in public procurement contracts.</p>
    </div>
</div>
<div class="container">
    <div id="login" class="col-md-6">
      <div class="container">
        <div class="col-md-8 col-md-offset-2">
          <form class="form-signin">
              <h2>Log in to your account</h2>
              <p>{toolname} is for registered users only.</p>
              <label for="inputEmail" class="sr-only">Email address</label>
              <input type="email" id="inputEmail" class="form-control" placeholder="Email address" required autofocus>
              <label for="inputPassword" class="sr-only">Password</label>
              <input type="password" id="inputPassword" class="form-control" placeholder="Password" required>
              {{#link-to "account.reset" class="btn-link"}}Lost password{{/link-to}}
              <div class="checkbox">
                  <label>
                      <input type="checkbox" value="remember-me"> Remember me
                  </label>
              </div>
              {{#link-to "graph" class="btn btn-lg btn-primary btn-block"}}Sign in{{/link-to}}
          </form>
        </div>
      </div>
    </div>
    <div id="register" class="col-md-6">
        <div class="col-md-8 col-md-offset-2">
          <form class="form-signin">
              <h2>Don't have an account?</h2>
              <p>Create one here. We will send the further instructions to your email. Your data are safe with us.</p>
              <label for="inputEmail" class="sr-only">Email address</label>
              <input type="email" id="inputEmail" class="form-control" placeholder="Email address" required autofocus>
              <br>
              {{#link-to "graph" class="btn btn-lg btn-primary btn-block"}}Create an account{{/link-to}}
          </form>
        </div>
    </div>
</div>
{{outlet}}
