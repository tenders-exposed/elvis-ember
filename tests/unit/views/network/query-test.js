import { moduleFor, test } from 'ember-qunit';

moduleFor('view:network/query', 'Unit | View | network/query');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
